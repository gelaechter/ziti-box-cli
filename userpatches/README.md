# Armbian Userpatches

This directory contains userpatches for the armbian build framework. They allow us to create a custom Armbian Image preconfigured as a Ziti Box.

## Usage



Run using:
```bash
./compile.sh build zitibox
```